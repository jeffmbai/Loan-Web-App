// event listener to handle the toggle
/*
const tglbtn = document.getElementById('toggle-btn-img');
tglbtn.addEventListener('click', menuRender, false);
const pageContent = document.getElementById('main-content');

function menuRender(e){
	const showHideMaenu = document.getElementById('content');
	showHideMaenu.classList.toggle('show');
	pageContent.classList.toggle('hide');
}

//displaying loan request form dynamically

const loanRequest = document.getElementById('btn-request-loan');
loanRequest.addEventListener('click', getLoanForm, false);


// get the back button to cancel request
const cancelLoanRequest = document.getElementById('to-index');

// function to get the loan form dynamicaly when the button is clicked
function getLoanForm(e){
	const loanForm = document.getElementById('loan-request-form');
	loanForm.classList.toggle('show');

	// function to create new elements
	function createNewElements(elem, text){
		const el = document.createElement(elem);
		el.textContent = text;

		return el;
	}
	// invoke the create element function
	const loanDiv = document.getElementById('btn-loan');
	pageContent.replaceChild(createNewElements('h5', 'Tujijenge Pamoja'),loanDiv);
	// display back button
	cancelLoanRequest.style.display ='block';
}*/